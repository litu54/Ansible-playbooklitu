---
  - name: creating directory under /tmp 
    hosts: vm_server 
    gather_facts: yes
    tasks:
      - name: creating an directory example under /tmp
        file:
         path: /tmp/example
         state: directory
   
